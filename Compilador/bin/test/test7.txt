app igauladez
 int a;
 start

 read(a);
 
 if ( a=10 ) then
	 write({Numero igual a 10} );
 else
	 if ( a<10 ) then
	 	write({Numero menor a 10} );
	 else
	 	write({Numero maior a 10} );
	 end;
 end;
 
 stop
